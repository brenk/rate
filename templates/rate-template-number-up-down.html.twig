{#
/**
 * @file
 * Default theme implementation for up/down voting display.
 *
 * Available variables:
 * - identifier: Outer div ID for AJAX purposes.
 * - up_button: Button to vote up.
 * - down_button: Button to vote down.
 * - score_class: Extra class to distinguish score level.
 * - score: Number of up votes.
 * - undo: Link to undo placed vote.
 * - info: Short text to display.
 * - description: Longer descriptive info to display.
 *
 * @ingroup themeable
 */
#}
<div id={{ identifier }} class="rate-widget-number-up-down">
  {{ up_button }}
  <div class="rate-number-up-down-rating {{ score_class }}">
    {{ score }}
  </div>
  {{ down_button }}
  {% if undo %}
    <div class="rate-undo-vote">{{ undo }}</div>
  {% endif %}
  {% if info %}
    <div class="rate-info">{{ info }}</div>
  {% endif %}
  {% if description %}
    <div class="rate-description">{{ description }}</div>
  {% endif %}
</div>
