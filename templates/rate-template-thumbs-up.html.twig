{#
/**
 * @file
 * Default theme implementation for up/down voting display.
 *
 * Available variables:
 * - identifier: Outer div ID for AJAX purposes.
 * - up_button: Up text/image for up vote.
 * - up_votes: Number of votes.
 * - undo: Link to undo placed vote.
 * - info: Optional brief text to present with voting widgets.
 * - description: Optional longer description to present with voting widgets.
 *
 * @ingroup themeable
 */
#}
<div id={{ identifier }} class="rate-widget-thumbs-up">
{{ up_button }}
<div class="rate-score">
  {{ up_votes }}
</div>
{% if undo %}
  <div class="rate-undo-vote">{{ undo }}</div>
{% endif %}
{% if info %}
  <div class="rate-info">{{ info }}</div>
{% endif %}
{% if description %}
  <div class="rate-description">{{ description }}</div>
{% endif %}
</div>
