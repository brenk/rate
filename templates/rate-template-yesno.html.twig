{#
/**
 * @file
 * Default theme implementation for yes/no voting display.
 *
 * Available variables:
 * - identifier: Outer div ID for AJAX purposes.
 * - yes_button: Up/yes text/image for up vote.
 * - up_votes: Number of up/yes votes.
 * - no_button: Down/no text/image for down vote.
 * - down_votes: Number of down/no votes.
 * - undo: Link to undo placed vote.
 * - info: Optional brief text to present with voting widgets.
 * - description: Optional longer description to present with voting widgets.
 *
 * @ingroup themeable
 */
#}
<div id={{ identifier }} class="rate-widget-yesno">
  <div class="item-list">
    <ul>
      <li class="rate-yes-up">
        {{ yes_button }}
        <div class="rate-yesno-votes">{{ up_votes }}</div>
      </li>
      <li class="rate-yes-down">
        {{ no_button }}
        <div class="rate-yesno-votes">{{ down_votes }}</div>
      </li>
    </ul>
  </div>
  {% if undo %}
    <div class="rate-undo-vote">{{ undo }}</div>
  {% endif %}
  {% if info %}
    <div class="rate-info">{{ info }}</div>
  {% endif %}
  {% if description %}
    <div class="rate-description">{{ description }}</div>
  {% endif %}
</div>
